from random import randint

class Die():
    """Класс представляюлщий один кубик"""
    def __init__(self, num_sides=6):
        self.num_sides =  num_sides

    def roll(self):
        """Возвращает одно случайное число от 1 до граней"""
        return randint(1, self.num_sides)
        
